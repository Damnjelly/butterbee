import butterbee/driver.{type WebDriver}
import butterbee/internal/bidi/browsing_context.{type Locator}

pub type By

pub fn locator(value: String) -> #(WebDriver, Locator) {
  todo
}

pub fn locators(value: String) -> #(WebDriver, List(Locator)) {
  todo
}
