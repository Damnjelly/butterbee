{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "firefox",
            "type": "shell",
            "command": "sh -c 'firefox --remote-debugging-port & gleam test'"
        },
        {
            "label": "servo",
            "type": "shell",
            "command": "sh -c 'servo --webdriver=9222 --headless & gleam test'"
        }
    ]
}
