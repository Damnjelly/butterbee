{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "gleam run"
        },
        {
            "label": "test",
            "type": "shell",
            "command": "gleam test"
        }, 
        {
            "label": "firefox",
            "type": "shell",
            "command": "sh -c 'firefox --headless --remote-debugging-port & sleep 0.5; gleam test'"
        }, 
        {
            "label": "firefox headed",
            "type": "shell",
            "command": "sh -c 'firefox --remote-debugging-port & sleep 0.5; gleam test'"
        }, 
        {
            "label": "firefox no sleep",
            "type": "shell",
            "command": "sh -c 'firefox --remote-debugging-port & gleam test'"
        }
    ]
}
